# CLAUDE.md — BTM

This file provides guidance for AI assistants (including Claude) working in this repository.

## Project Overview

**BTM** is a new project under `jaydmcchesney/BTM`. The repository is in its initial state — update this section as the project scope, purpose, and tech stack are defined.

## Repository Structure

```
BTM/
├── CLAUDE.md          # AI assistant guidance (this file)
└── (project files)    # To be added
```

Update this tree as the project structure evolves.

## Development Setup

### Prerequisites

<!-- Update with actual requirements as the project takes shape -->
- Git

### Getting Started

```bash
git clone <repo-url>
cd BTM
# Add setup steps here (e.g., npm install, pip install, cargo build)
```

## Build & Run

<!-- Fill in once a build system is chosen -->
```bash
# Build: <command>
# Run:   <command>
# Test:  <command>
# Lint:  <command>
```

## Key Commands

| Task     | Command |
|----------|---------|
| Build    | TBD     |
| Test     | TBD     |
| Lint     | TBD     |
| Format   | TBD     |

## Architecture

<!-- Describe the high-level architecture once established -->

## Conventions

### Git

- Branch names: use descriptive feature branch names (e.g., `feature/add-auth`, `fix/login-bug`)
- Commit messages: use imperative mood, concise subject line (< 72 chars), and a body for context when needed
- Keep commits atomic — one logical change per commit

### Code Style

<!-- Update once linting/formatting tools are configured -->
- Follow the established linter and formatter configurations
- Be consistent with existing patterns in the codebase

### Testing

<!-- Update once testing framework is chosen -->
- Write tests for new functionality
- Ensure all tests pass before pushing

## AI Assistant Guidelines

When working in this repository:

1. **Read before modifying** — always read a file before editing it
2. **Run tests** — verify changes don't break existing functionality
3. **Follow existing patterns** — match the style and conventions already in use
4. **Keep changes minimal** — only modify what's necessary for the task
5. **Update this file** — when adding new tools, commands, or conventions, update CLAUDE.md to keep it current
